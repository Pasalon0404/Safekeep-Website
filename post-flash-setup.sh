#!/bin/bash
# THIS SCRIPT IS NO LONGER NEEDED.
#
# The new build.sh produces an .img file with pre-baked partitions.
# Users simply flash safekeep.img to USB and boot — the data partition
# is already created and ready for the Setup Wizard (Ctrl+Alt+S).
#
# This file can be safely deleted.
echo "This script is no longer needed."
echo "Just flash safekeep.img to your USB and boot."
